# 新手流程（问剑长生）

> 这个文件是**这条用例的门面**：起跑线在哪、要跑到哪、跑到哪一步了、上次为什么挂。
> 人看它，智能体也看它 —— 别让它过期。

## 基本

| 项 | 值 |
|---|---|
| 平台里的用例 | `新手流程总脚本（问剑长生）`，id `b3f642f0-3a98-4ee6-b971-16a60d0fe771` |
| 模块 | 新手流程 |
| 入口 | 库里的内容是一个 loader，运行时读 `cases/新手流程/dist/新手流程总脚本.py` 执行 |
| 产物 | `cases/新手流程/dist/新手流程总脚本.py`（1802 行，由 `lib/` + 本用例正文构建） |
| 分段稿 | `dist/run_flow2.py`、`run_stage2a/2b/3a/3b.py`（每段自带起跑线，用于单次跑不完时接续） |

## 起跑线（人工复位，平台不复位也不检查）

- **场景**：`Assets/Scenes/main.unity`
- **标志物**：`GameRoot/Canvas2D/Normal/BootWindow(Clone)`（启动界面）
- 也就是：游戏停在**启动界面**（未进游戏）。跑之前请自行把游戏恢复到这个状态。

## 目标

跑到**「吃筑基丹前」**为止。

## 覆盖进度

| 段 | 内容 | 状态 |
|---|---|---|
| STAGE 1 | 登出 → 注册并登录 → 创角改名 → 启程 → 新手主界面 | ✅ **跑通两次**（2026-09-23 17:50 / 18:30，含创角"长歌岳""照野川"） |
| STAGE 2A | 寻找道院 → 探雾 → 世界地图寻路 → 修炼/聚灵/丹药 | ⚠️ 推进到"点地图格 57003 / SKIP"后失败 |
| STAGE 2B | 加入道院（真武）→ 洞府升级 | ❌ 未跑通 |
| STAGE 3 | 聚灵塔试炼 → 外院晋升 → 门派比试 → 收尾 | ❌ 未跑通 |

## 上一次运行（2026-09-23 21:00，本地）

- **走到**：STAGE 1 第 8 步（点完"注册并登录"）
- **挂在**：等 `CreateCharacterWindow(Clone)` 200 秒超时（"期间推进剧情 0 下"）
- **游戏侧报错**：`尝试获远端配置失败！未初始化！！ key:mini_extra_begin_version`
- **判定**：**环境问题，不是用例问题**（同一天的两次失败也都是游戏侧：18:30 那次是
  `获取服务器时间失败 / ConnectionError → 127.0.0.1:5003`）
- **现状**：`:5003` 已恢复（HTTP 200）
- **注意**：平台把这次失败判成了 `case`（脚本状态被标 `broken`），这是分类器不认识
  游戏侧失败指纹导致的误判，别被它带偏。

## 下一步

1. 在 Unity 里确认游戏能手动走到**创角界面**（若手动也进不去，就是游戏/服务端的问题）
2. 复位到启动界面 → 跑 `dist/新手流程总脚本.py`（经平台，别直接跑文件）
3. 挂了看 `failure.kind`：`case` 才改用例；`environment` 停下报环境

## 失败现场在哪

每次运行的目录：`workspace/default/unity-auto/b3f642f0-.../<时间戳>_<名字>/`
—— `failure.txt`（走到哪一步 + 控制台错误）、`failure.png`、`steps.jsonl`（步骤轨迹）、
`run.mp4`（录像）、`case.py`（当次实际执行的脚本）。
