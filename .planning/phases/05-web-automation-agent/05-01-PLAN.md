---
phase: 05-web-automation-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/agents/web/tools.py
  - src/app/core/config.py
  - workspace/web/skills/playwright-cli/SKILL.md
  - workspace/web/skills/playwright-cli/references/element-attributes.md
  - workspace/web/skills/playwright-cli/references/playwright-tests.md
  - workspace/web/skills/playwright-cli/references/request-mocking.md
  - workspace/web/skills/playwright-cli/references/running-code.md
  - workspace/web/skills/playwright-cli/references/session-management.md
  - workspace/web/skills/playwright-cli/references/storage-state.md
  - workspace/web/skills/playwright-cli/references/test-generation.md
  - workspace/web/skills/playwright-cli/references/tracing.md
  - workspace/web/skills/playwright-cli/references/video-recording.md
  - workspace/web/skills/agent-browser/SKILL.md
  - workspace/web/skills/agent-browser-vs-playwright-cli/SKILL.md
  - workspace/web/skills/pw-dogfood/SKILL.md
  - workspace/web/skills/pw-dogfood/references/accessibility-testing.md
  - workspace/web/skills/pw-dogfood/references/issue-taxonomy.md
  - workspace/web/skills/pw-dogfood/references/performance-testing.md
  - workspace/web/skills/pw-dogfood/references/security-checks.md
  - workspace/web/skills/pw-dogfood/templates/report-template.md
  - workspace/web/skills/component-aware-web-automation/SKILL.md
  - workspace/web/skills/component-aware-web-automation/references/script-analyst-guide.md
  - workspace/web/skills/component-aware-web-automation/references/stage-manager-guide.md
  - workspace/web/skills/component-aware-web-automation/references/blocking-coach-guide.md
  - workspace/web/skills/component-aware-web-automation/references/set-designer-guide.md
  - workspace/web/skills/component-aware-web-automation/references/choreographer-guide.md
  - workspace/web/skills/component-aware-web-automation/references/assistant-director-guide.md
  - workspace/web/skills/component-aware-web-automation/references/continuity-lead-guide.md
  - workspace/web/ARTIFACT_CONTRACT.md
autonomous: true
requirements:
  - WEB-02
  - WEB-03
  - WEB-04
  - WEB-05
  - WEB-07

must_haves:
  truths:
    - "detect_test_mode tool correctly classifies URL input as MODE_A_QA, repo input as MODE_B_COMPONENT, and ambiguous input as ASK_CLARIFICATION"
    - "check_environment tool reports playwright-cli and agent-browser availability"
    - "ensure_output_dir creates MODE_A_QA directory with screenshots/traces/videos/storage subdirs and MODE_B_COMPONENT directory with poms/tests/references subdirs"
    - "All 5 Skill directories exist with SKILL.md files readable by FilesystemBackend"
    - "CompositeBackend routes file operations to FilesystemBackend and execute operations to LocalShellBackend"
  artifacts:
    - path: "src/app/agents/web/tools.py"
      provides: "Custom tools and backend configuration"
      exports: ["detect_test_mode", "check_environment", "ensure_output_dir", "composite_backend", "file_backend", "shell_backend", "output_root"]
    - path: "src/app/core/config.py"
      provides: "Updated settings with graphify_mcp_command, graphify_mcp_args"
      contains: "graphify_mcp"
    - path: "workspace/web/skills/playwright-cli/SKILL.md"
      provides: "Playwright CLI usage guide"
      min_lines: 50
    - path: "workspace/web/skills/pw-dogfood/SKILL.md"
      provides: "Professional QA 6-phase workflow"
      min_lines: 50
    - path: "workspace/web/skills/component-aware-web-automation/SKILL.md"
      provides: "7-Agent Director Pipeline guide"
      min_lines: 50
    - path: "workspace/web/ARTIFACT_CONTRACT.md"
      provides: "Output artifact format specification"
      contains: "Mode A"
  key_links:
    - from: "src/app/agents/web/tools.py"
      to: "src/app/core/config.py"
      via: "from app.core.config import settings"
      pattern: "from app\\.core\\.config import settings"
    - from: "src/app/agents/web/tools.py"
      to: "deepagents.backends"
      via: "CompositeBackend, FilesystemBackend, LocalShellBackend imports"
      pattern: "from deepagents\\.backends import"
---

<objective>
Create the foundational backend artifacts for the Web Automation Agent: custom tools (detect_test_mode, check_environment, ensure_output_dir), CompositeBackend configuration, updated config.py settings, and all 5 Skill directories with SKILL.md files and references copied from the classroom reference.

Purpose: These are the building blocks that agent.py (Plan 02) will assemble into a working Web Agent. Without these tools and Skills, the agent has no mode detection, no environment checking, no output directory structure, and no domain knowledge.

Output: tools.py with 3 custom tools + CompositeBackend; config.py with Graphify settings; 5 Skill directories (25+ files) with SKILL.md and reference guides; ARTIFACT_CONTRACT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-automation-agent/05-CONTEXT.md
@.planning/phases/05-web-automation-agent/05-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/core/config.py (current):
```python
class Settings(BaseSettings):
    deepseek_api_key: str = ""
    doubao_api_key: str = ""
    openai_api_key: str = ""
    enable_pdf_multimodal: bool = True
    langgraph_api_url: str = "http://localhost:2026"
    workspace_dir: Path = Path(__file__).parent.parent.parent.parent / "workspace"
    # LightRAG settings...
    # MCP settings (docling, wiki-mcp)...
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}
settings = Settings()
```

From src/app/agents/web/agent.py (current stub):
```python
workspace_dir = Path(__file__).parent.parent.parent.parent.parent / "workspace"
file_backend = FilesystemBackend(root_dir=workspace_dir, virtual_mode=True)
```

From src/app/agents/testcase/agent.py (established pattern):
```python
from deepagents import create_deep_agent as create_agent
from deepagents.backends import FilesystemBackend
from deepagents.middleware import SkillsMiddleware
from app.core.config import settings
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tools.py with custom tools and CompositeBackend</name>
  <files>src/app/agents/web/tools.py</files>
  <read_first>
    - D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/web/tools.py (classroom reference -- primary source)
    - src/app/core/config.py (import settings for workspace_dir)
    - src/app/agents/web/agent.py (current stub -- understand workspace_dir pattern)
  </read_first>
  <action>
    Create `src/app/agents/web/tools.py` by adapting the classroom reference `tools.py`. The key adaptation is replacing the hardcoded workspace path with `settings.workspace_dir / "web"` to match the project's config pattern.

    Specific contents:
    1. Module docstring: "Standalone tools and backend configuration for the Web Automation Testing Agent."
    2. Imports: `os, re, subprocess` from stdlib; `datetime.datetime, Path` from pathlib; `Any` from typing; `CompositeBackend, FilesystemBackend, LocalShellBackend` from `deepagents.backends`; `settings` from `app.core.config`.
    3. Workspace paths:
       - `workspace_dir = settings.workspace_dir / "web"` (NOT hardcoded path like classroom)
       - `output_root = workspace_dir / "web-output"`
       - `output_root.mkdir(parents=True, exist_ok=True)`
    4. `detect_test_mode(user_request: str) -> str` function:
       - Regex for URL: `re.compile(r"https?://[^\s\"']+")`
       - Path markers list exactly as classroom: `r"[a-zA-Z]:\\", r"/home/", r"/Users/", r"/workspace/", r"git@", r"github\.com", r"\.git", r"src/", r"repo", r"project path", r"source code", r"codebase"`
       - Logic: has_repo and not has_url -> "MODE_B_COMPONENT"; has_url and not has_repo -> "MODE_A_QA"; both -> "MODE_B_COMPONENT"; neither -> "ASK_CLARIFICATION"
    5. `check_environment() -> dict[str, Any]` function:
       - Identical to classroom reference: iterate `["playwright-cli", "agent-browser"]`, try `subprocess.run([cmd, "--version"], ...)`, handle `FileNotFoundError` with `.cmd` fallback on Windows (`os.name == "nt"`), return dict with platform and tools status.
    6. `ensure_output_dir(mode: str, label: str = "") -> str` function:
       - Identical to classroom: timestamp `YYYYMMDD_HHMMSS`, safe_label via `re.sub(r"[^\w\-]", "_", label)[:40]` or "session".
       - MODE_A_QA: creates `output_root / "qa" / f"{safe_label}_{ts}"` with subdirs `("screenshots", "traces", "videos", "storage")`.
       - MODE_B_COMPONENT: creates `output_root / "tests" / f"{safe_label}_{ts}"` with subdirs `("poms", "tests", "references")`.
       - Raises `ValueError` for unknown mode.
    7. Backend factory:
       - `shell_backend = LocalShellBackend(root_dir=workspace_dir, virtual_mode=False, inherit_env=True, timeout=180)`
       - `file_backend = FilesystemBackend(root_dir=workspace_dir, virtual_mode=True)`
       - `composite_backend = CompositeBackend(default=shell_backend, routes={"/": file_backend})`
       - Use module-level variables (not factory function), matching classroom reference's `shell_backend, file_backend, composite_backend = create_backends()` pattern but as direct assignments.
  </action>
  <acceptance_criteria>
    - `grep -c "def detect_test_mode" src/app/agents/web/tools.py` returns 1
    - `grep -c "def check_environment" src/app/agents/web/tools.py` returns 1
    - `grep -c "def ensure_output_dir" src/app/agents/web/tools.py` returns 1
    - `grep "MODE_A_QA" src/app/agents/web/tools.py` has at least 3 matches (return value, ensure_output_dir if branch)
    - `grep "MODE_B_COMPONENT" src/app/agents/web/tools.py` has at least 3 matches
    - `grep "ASK_CLARIFICATION" src/app/agents/web/tools.py` has at least 1 match
    - `grep "CompositeBackend" src/app/agents/web/tools.py` has at least 2 matches (import + instantiation)
    - `grep "LocalShellBackend" src/app/agents/web/tools.py` has at least 2 matches
    - `grep "FilesystemBackend" src/app/agents/web/tools.py` has at least 2 matches
    - `grep "from app.core.config import settings" src/app/agents/web/tools.py` returns 1
    - `python -c "from app.agents.web.tools import detect_test_mode; print(detect_test_mode('Test https://example.com'))"` returns "MODE_A_QA"
    - `python -c "from app.agents.web.tools import detect_test_mode; print(detect_test_mode('C:\\Projects\\app'))"` returns "MODE_B_COMPONENT"
    - `python -c "from app.agents.web.tools import detect_test_mode; print(detect_test_mode('hello'))"` returns "ASK_CLARIFICATION"
  </acceptance_criteria>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && python -c "from app.agents.web.tools import detect_test_mode, check_environment, ensure_output_dir, composite_backend, file_backend, shell_backend, output_root; assert detect_test_mode('https://example.com') == 'MODE_A_QA'; assert detect_test_mode('C:\\\\proj') == 'MODE_B_COMPONENT'; assert detect_test_mode('hello') == 'ASK_CLARIFICATION'; env = check_environment(); assert 'tools' in env; print('ALL TOOL CHECKS PASSED')"</automated>
  </verify>
  <done>tools.py exports detect_test_mode, check_environment, ensure_output_dir, composite_backend, file_backend, shell_backend, output_root. All three tools are functional. CompositeBackend routes correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Copy 5 Skill directories and ARTIFACT_CONTRACT.md from classroom reference + update config.py</name>
  <files>
    - workspace/web/skills/playwright-cli/SKILL.md
    - workspace/web/skills/playwright-cli/references/*.md
    - workspace/web/skills/agent-browser/SKILL.md
    - workspace/web/skills/agent-browser-vs-playwright-cli/SKILL.md
    - workspace/web/skills/pw-dogfood/SKILL.md
    - workspace/web/skills/pw-dogfood/references/*.md
    - workspace/web/skills/pw-dogfood/templates/report-template.md
    - workspace/web/skills/component-aware-web-automation/SKILL.md
    - workspace/web/skills/component-aware-web-automation/references/*.md
    - workspace/web/ARTIFACT_CONTRACT.md
    - src/app/core/config.py
  </files>
  <read_first>
    - D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/web/skills/ (all SKILL.md and reference files -- primary source to copy)
    - D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/web/ARTIFACT_CONTRACT.md (primary source to copy)
    - src/app/core/config.py (file to modify for Graphify settings)
  </read_first>
  <action>
    **Part A: Copy Skills from classroom reference**

    Copy all 5 Skill directories from the classroom reference to `workspace/web/skills/`:
    - Source: `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/web/skills/`
    - Destination: `D:/test_agent/smart-test-platform/workspace/web/skills/`

    Execute: `cp -r "D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/web/skills/" "D:/test_agent/smart-test-platform/workspace/web/skills/"`

    Also copy ARTIFACT_CONTRACT.md:
    - Source: `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/web/ARTIFACT_CONTRACT.md`
    - Destination: `D:/test_agent/smart-test-platform/workspace/web/ARTIFACT_CONTRACT.md`

    After copy, verify the directory structure matches exactly:
    ```
    workspace/web/skills/
    ├── playwright-cli/SKILL.md + 9 references/*.md
    ├── agent-browser/SKILL.md
    ├── agent-browser-vs-playwright-cli/SKILL.md
    ├── pw-dogfood/SKILL.md + 4 references/*.md + templates/report-template.md
    └── component-aware-web-automation/SKILL.md + 7 references/*.md
    ```

    **Part B: Update config.py**

    Read `src/app/core/config.py` and add Graphify MCP settings after the wiki-mcp settings block (after line 29, before `model_config`). Add exactly these fields:

    ```python
    # Graphify MCP (Phase 5 - Web Agent component-aware mode)
    graphify_mcp_command: str = "npx"
    graphify_mcp_args: str = "graphify serve"
    ```

    Do NOT modify any existing fields. Only add the two new Graphify fields.
  </action>
  <acceptance_criteria>
    - `ls workspace/web/skills/playwright-cli/SKILL.md` exists
    - `ls workspace/web/skills/agent-browser/SKILL.md` exists
    - `ls workspace/web/skills/agent-browser-vs-playwright-cli/SKILL.md` exists
    - `ls workspace/web/skills/pw-dogfood/SKILL.md` exists
    - `ls workspace/web/skills/component-aware-web-automation/SKILL.md` exists
    - `ls workspace/web/skills/pw-dogfood/templates/report-template.md` exists
    - `ls workspace/web/skills/component-aware-web-automation/references/script-analyst-guide.md` exists
    - `ls workspace/web/skills/component-aware-web-automation/references/continuity-lead-guide.md` exists
    - `ls workspace/web/ARTIFACT_CONTRACT.md` exists
    - `find workspace/web/skills/ -name "*.md" | wc -l` returns at least 25
    - `grep "graphify_mcp_command" src/app/core/config.py` returns 1 match
    - `grep "graphify_mcp_args" src/app/core/config.py` returns 1 match
    - All existing config.py fields still present (deepseek_api_key, wiki_mcp_command, etc.)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && python -c "
import os
skills_dir = 'workspace/web/skills'
required = ['playwright-cli', 'agent-browser', 'agent-browser-vs-playwright-cli', 'pw-dogfood', 'component-aware-web-automation']
for s in required:
    skill_path = os.path.join(skills_dir, s, 'SKILL.md')
    assert os.path.exists(skill_path), f'Missing: {skill_path}'
refs = ['script-analyst-guide.md', 'stage-manager-guide.md', 'blocking-coach-guide.md', 'set-designer-guide.md', 'choreographer-guide.md', 'assistant-director-guide.md', 'continuity-lead-guide.md']
for r in refs:
    ref_path = os.path.join(skills_dir, 'component-aware-web-automation', 'references', r)
    assert os.path.exists(ref_path), f'Missing: {ref_path}'
assert os.path.exists('workspace/web/ARTIFACT_CONTRACT.md'), 'Missing ARTIFACT_CONTRACT.md'
from app.core.config import settings
assert hasattr(settings, 'graphify_mcp_command'), 'Missing graphify_mcp_command'
assert hasattr(settings, 'graphify_mcp_args'), 'Missing graphify_mcp_args'
print('ALL SKILL + CONFIG CHECKS PASSED')
"</automated>
  </verify>
  <done>5 Skill directories with SKILL.md + references copied to workspace/web/skills/. ARTIFACT_CONTRACT.md copied. config.py has graphify_mcp_command and graphify_mcp_args fields. All 7 component-aware reference guides present.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.agents.web.tools import detect_test_mode, check_environment, ensure_output_dir"` -- tools import cleanly
2. `python -c "from app.agents.web.tools import composite_backend, file_backend, shell_backend"` -- backends instantiate
3. `python -c "from app.core.config import settings; assert hasattr(settings, 'graphify_mcp_command')"` -- config updated
4. All 5 SKILL.md files exist and are readable
5. All 7 component-aware reference guides exist
6. ARTIFACT_CONTRACT.md exists
</verification>

<success_criteria>
- tools.py exports all 7 names (3 tools + 4 backend/path variables)
- detect_test_mode returns correct mode for URL, repo path, and ambiguous input
- check_environment returns a dict with "platform" and "tools" keys
- ensure_output_dir creates correct subdirectory structure for both modes
- CompositeBackend routes correctly (default=shell, routes={"/": file})
- 5 Skill directories with SKILL.md files present
- 25+ reference/template files present across all Skills
- config.py has graphify_mcp_command and graphify_mcp_args fields added
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-automation-agent/05-01-SUMMARY.md`
</output>
