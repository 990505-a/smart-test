---
phase: 06-api-automation-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/agents/api/tools.py
  - src/app/agents/api/tools/__init__.py
  - src/app/agents/api/tools/api_parser.py
  - src/app/agents/api/tools/metrics.py
  - src/app/agents/api/tools/playwright_mcp_server.py
  - src/app/core/config.py
  - pyproject.toml
  - .gitignore
  - workspace/api/skills/test-scenario-design/SKILL.md
  - workspace/api/skills/playwright-api-testing/SKILL.md
  - workspace/api/skills/api-test-quality/SKILL.md
autonomous: true
requirements: [API-01, API-03, API-04, API-05, API-06, API-07, API-09]

must_haves:
  truths:
    - "api_parser.py resolves $ref references and extracts operations from OpenAPI JSON/YAML specs"
    - "metrics.py computes coverage (scenario, operation, usability) and validates TypeScript syntax"
    - "playwright_mcp_server.py loads Playwright MCP tools without crashing in LangGraph server"
    - "tools.py exports MASTEST_TOOLS list combining all three tool modules"
    - "CompositeBackend is configured with shell + file backends rooted at workspace/api/"
    - "3 Skill directories exist with valid SKILL.md files containing MASTEST methodology content"
    - "GitNexus MCP config fields exist in config.py"
    - "Levenshtein package is installed and importable"
  artifacts:
    - path: "src/app/agents/api/tools.py"
      provides: "Standalone tools module with CompositeBackend, independent of agent.py"
      exports: ["MASTEST_TOOLS", "composite_backend", "file_backend", "shell_backend"]
    - path: "src/app/agents/api/tools/api_parser.py"
      provides: "OpenAPI $ref resolution and operation extraction"
      exports: ["parse_api_spec", "parse_api_operations", "format_operations_for_prompt"]
    - path: "src/app/agents/api/tools/metrics.py"
      provides: "Deterministic coverage metrics and syntax validation"
      exports: ["check_script_syntax", "compute_coverage"]
    - path: "src/app/agents/api/tools/playwright_mcp_server.py"
      provides: "Playwright MCP tool loading via stdio"
      exports: ["playwright_api_tools"]
    - path: "src/app/core/config.py"
      provides: "GitNexus MCP config fields"
      contains: "gitnexus_mcp"
    - path: "workspace/api/skills/test-scenario-design/SKILL.md"
      provides: "Unit and system scenario generation skill"
      contains: "Unit Test Scenarios"
    - path: "workspace/api/skills/playwright-api-testing/SKILL.md"
      provides: "Playwright TypeScript script writing skill"
      contains: "request fixture"
    - path: "workspace/api/skills/api-test-quality/SKILL.md"
      provides: "Quality analysis and report template skill"
      contains: "Final Report Template"
  key_links:
    - from: "src/app/agents/api/tools.py"
      to: "src/app/agents/api/tools/api_parser.py"
      via: "from .api_parser import parse_api_spec"
      pattern: "from \\.api_parser import"
    - from: "src/app/agents/api/tools.py"
      to: "src/app/agents/api/tools/metrics.py"
      via: "from .metrics import check_script_syntax, compute_coverage"
      pattern: "from \\.metrics import"
    - from: "src/app/agents/api/tools.py"
      to: "src/app/agents/api/tools/playwright_mcp_server.py"
      via: "from .playwright_mcp_server import playwright_api_tools"
      pattern: "from \\.playwright_mcp_server import"
    - from: "src/app/agents/api/tools.py"
      to: "src/app/core/config.py"
      via: "from src.app.core.config import settings"
      pattern: "from src\\.app\\.core\\.config import"
---

<objective>
Create the backend foundation for the API Automation Agent: deterministic tools (api_parser, metrics, playwright_mcp_server), CompositeBackend configuration, GitNexus MCP settings, 3 Skill directories, and the Levenshtein dependency.

Purpose: Establish all tooling, backend, and skill infrastructure so Plan 02 can wire them into the agent with zero exploration needed.

Output: tools.py with MASTEST_TOOLS + CompositeBackend, 3 tool modules, 3 Skill SKILL.md files, config.py with GitNexus fields, Levenshtein installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-automation-agent/06-CONTEXT.md
@.planning/phases/06-api-automation-agent/06-RESEARCH.md

<interfaces>
<!-- Phase 5 patterns that this plan MUST replicate exactly -->

From src/app/agents/web/tools.py (CompositeBackend pattern):
```python
from deepagents.backends import CompositeBackend, FilesystemBackend, LocalShellBackend
from src.app.core.config import settings

workspace_dir = settings.workspace_dir / "web"  # API agent uses "api" instead

shell_backend = LocalShellBackend(
    root_dir=workspace_dir,
    virtual_mode=False,
    inherit_env=True,
    timeout=180,
)

file_backend = FilesystemBackend(
    root_dir=workspace_dir,
    virtual_mode=True,
)

composite_backend = CompositeBackend(
    default=shell_backend,
    routes={"/": file_backend},
)
```

From src/app/core/config.py (settings pattern):
```python
class Settings(BaseSettings):
    # MCP fields pattern:
    graphify_mcp_command: str = "npx"
    graphify_mcp_args: str = "graphify serve"
    # GitNexus follows same pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools.py with CompositeBackend + 3 tool modules</name>
  <files>
    src/app/agents/api/tools.py,
    src/app/agents/api/tools/__init__.py,
    src/app/agents/api/tools/api_parser.py,
    src/app/agents/api/tools/metrics.py,
    src/app/agents/api/tools/playwright_mcp_server.py,
    src/app/core/config.py,
    pyproject.toml
  </files>
  <read_first>
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/__init__.py
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/api_parser.py
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/metrics.py
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/playwright_mcp_server.py
    src/app/agents/web/tools.py
    src/app/core/config.py
    pyproject.toml
  </read_first>
  <acceptance_criteria>
    - `src/app/agents/api/tools.py` exists and contains `from src.app.core.config import settings`
    - `src/app/agents/api/tools.py` contains `workspace_dir = settings.workspace_dir / "api"` (no hardcoded paths)
    - `src/app/agents/api/tools.py` contains `composite_backend = CompositeBackend(default=shell_backend, routes={"/": file_backend})`
    - `src/app/agents/api/tools.py` contains `MASTEST_TOOLS` list with `parse_openapi_spec`, `check_script_syntax`, `compute_coverage`
    - `src/app/agents/api/tools/api_parser.py` contains `def parse_api_spec(spec_url_or_path: str) -> dict[str, Any]:`
    - `src/app/agents/api/tools/api_parser.py` contains `def _resolve_all_refs(obj, spec):` for $ref resolution
    - `src/app/agents/api/tools/metrics.py` contains `import Levenshtein`
    - `src/app/agents/api/tools/metrics.py` contains `def check_script_syntax(script: str) -> str:`
    - `src/app/agents/api/tools/metrics.py` contains `def compute_coverage(` with `parsed_api_json` parameter
    - `src/app/agents/api/tools/playwright_mcp_server.py` contains `asyncio.new_event_loop().run_until_complete(client.get_tools())` (NOT `asyncio.run`)
    - `src/app/core/config.py` contains `gitnexus_mcp_command: str = "node"` and `gitnexus_mcp_args: str = "D:/prpm/72codegraph/gitnexus/dist/cli/index.js mcp"`
    - `pyproject.toml` contains `Levenshtein` in dependencies list
    - Command `.venv/Scripts/python -c "import Levenshtein; print(Levenshtein.__version__)"` succeeds
    - All import paths use `from src.app.` prefix (not `from app.`)
  </acceptance_criteria>
  <action>
    **Step 1: Install Levenshtein dependency**
    Run `.venv/Scripts/pip install Levenshtein` to install the package. Then add `"Levenshtein"` to the `dependencies` list in `pyproject.toml` (alphabetical order, after `langgraph`).

    **Step 2: Create tools directory structure**
    Create directory `src/app/agents/api/tools/` with `__init__.py`.

    **Step 3: Create src/app/agents/api/tools/api_parser.py**
    Copy from classroom reference `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/api_parser.py` VERBATIM. No changes needed -- this module has no project-specific paths or imports.

    **Step 4: Create src/app/agents/api/tools/metrics.py**
    Copy from classroom reference `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/api/tools/metrics.py` VERBATIM. No changes needed.

    **Step 5: Create src/app/agents/api/tools/playwright_mcp_server.py**
    Adapt from classroom reference. The CRITICAL change: replace `asyncio.run(client.get_tools())` with `asyncio.new_event_loop().run_until_complete(client.get_tools())`. This prevents crashes inside LangGraph server event loop (established Phase 3 pattern per STATE.md).

    Also remove the `mcp-server-chart` entry if it exists -- only keep `playwright-api`:
    ```python
    import asyncio
    from langchain_mcp_adapters.client import MultiServerMCPClient

    client = MultiServerMCPClient(
        {
            "playwright-api": {
                "transport": "stdio",
                "command": "npx",
                "args": ["-y", "@executeautomation/playwright-mcp-server"],
            },
        }
    )

    playwright_api_tools = asyncio.new_event_loop().run_until_complete(client.get_tools())
    ```

    **Step 6: Create src/app/agents/api/tools/__init__.py**
    Adapt from classroom reference. Change import paths from `from app.agents.api.tools.` to `from src.app.agents.api.tools.`:
    ```python
    from src.app.agents.api.tools.api_parser import parse_api_spec
    from src.app.agents.api.tools.metrics import check_script_syntax as _check_syntax
    from src.app.agents.api.tools.metrics import compute_coverage as _compute_coverage
    from src.app.agents.api.tools.playwright_mcp_server import playwright_api_tools
    ```
    Keep the `@tool` decorators and `MASTEST_TOOLS` list exactly as classroom reference.

    **Step 7: Create src/app/agents/api/tools.py (standalone module with backends)**
    This is the Phase 5 pattern -- tools.py at the agents/api/ level with backend configuration, separate from agent.py. Use the exact pattern from `src/app/agents/web/tools.py` but adapted for API:
    - `workspace_dir = settings.workspace_dir / "api"` (per D-05)
    - `from src.app.core.config import settings`
    - Module-level `shell_backend`, `file_backend`, `composite_backend` with same parameters as web/tools.py
    - Import and re-export `MASTEST_TOOLS` from `src.app.agents.api.tools`
    - Export `composite_backend`, `file_backend`, `shell_backend` for agent.py to import

    **Step 8: Update src/app/core/config.py**
    Add GitNexus MCP settings after the Graphify section (per D-09):
    ```python
    # GitNexus MCP (Phase 6 - API Agent code knowledge graph)
    gitnexus_mcp_command: str = "node"
    gitnexus_mcp_args: str = "D:/prpm/72codegraph/gitnexus/dist/cli/index.js mcp"
    ```
  </action>
  <verify>
    <automated>.venv/Scripts/python -m pytest tests/test_api_tools.py -x 2>/dev/null || echo "Test file not yet created -- verify imports manually:"; .venv/Scripts/python -c "from src.app.agents.api.tools import MASTEST_TOOLS, composite_backend, file_backend; print(f'Tools: {len(MASTEST_TOOLS)}, Backend: {type(composite_backend).__name__}')" && echo "IMPORTS OK"</automated>
  </verify>
  <done>
    - tools.py exports MASTEST_TOOLS, composite_backend, file_backend, shell_backend
    - api_parser.py resolves $ref and extracts operations (copied verbatim from classroom)
    - metrics.py computes coverage and validates syntax (copied verbatim from classroom)
    - playwright_mcp_server.py uses asyncio.new_event_loop() (NOT asyncio.run)
    - All imports use `from src.app.` prefix
    - config.py has gitnexus_mcp_command and gitnexus_mcp_args fields
    - Levenshtein installed and in pyproject.toml
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 3 Skill directories + update .gitignore</name>
  <files>
    workspace/api/skills/test-scenario-design/SKILL.md,
    workspace/api/skills/playwright-api-testing/SKILL.md,
    workspace/api/skills/api-test-quality/SKILL.md,
    .gitignore
  </files>
  <read_first>
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/test-scenario-design/SKILL.md
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/playwright-api-testing/SKILL.md
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/api-test-quality/SKILL.md
    .gitignore
  </read_first>
  <acceptance_criteria>
    - `workspace/api/skills/test-scenario-design/SKILL.md` exists with `name: test-scenario-design` in frontmatter
    - `workspace/api/skills/test-scenario-design/SKILL.md` contains "Unit Test Scenarios" section header
    - `workspace/api/skills/test-scenario-design/SKILL.md` contains "System Test Scenarios" section header
    - `workspace/api/skills/playwright-api-testing/SKILL.md` exists with `name: playwright-api-testing` in frontmatter
    - `workspace/api/skills/playwright-api-testing/SKILL.md` contains "request fixture" reference
    - `workspace/api/skills/playwright-api-testing/SKILL.md` contains "test.step" reference
    - `workspace/api/skills/api-test-quality/SKILL.md` exists with `name: api-test-quality` in frontmatter
    - `workspace/api/skills/api-test-quality/SKILL.md` contains "Final Report Template" section
    - `workspace/api/skills/api-test-quality/SKILL.md` contains "compute_coverage" reference
    - `.gitignore` contains line `!workspace/api/skills/` (negation rule)
    - `git status` shows all 3 SKILL.md files as tracked
  </acceptance_criteria>
  <action>
    **Step 1: Create workspace/api/skills/test-scenario-design/SKILL.md**
    Copy VERBATIM from `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/test-scenario-design/SKILL.md`. No changes needed.

    **Step 2: Create workspace/api/skills/playwright-api-testing/SKILL.md**
    Copy VERBATIM from `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/playwright-api-testing/SKILL.md`. No changes needed.

    **Step 3: Create workspace/api/skills/api-test-quality/SKILL.md**
    Copy VERBATIM from `D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/workspace/api/skills/api-test-quality/SKILL.md`. No changes needed.

    **Step 4: Update .gitignore**
    Add `!workspace/api/skills/` negation rule after the existing `!workspace/web/skills/` line. This ensures Skill source files are tracked despite `workspace/*/` being ignored.

    **Step 5: Force-add Skill files to git tracking**
    Run `git add -f workspace/api/skills/` to ensure the files are tracked despite the gitignore pattern.
  </action>
  <verify>
    <automated>.venv/Scripts/python -c "
import os
skills = [
    'workspace/api/skills/test-scenario-design/SKILL.md',
    'workspace/api/skills/playwright-api-testing/SKILL.md',
    'workspace/api/skills/api-test-quality/SKILL.md',
]
for s in skills:
    assert os.path.exists(s), f'Missing: {s}'
    with open(s) as f:
        content = f.read()
        assert 'name:' in content, f'No frontmatter in {s}'
print('All 3 Skills verified OK')
" && grep -q '!workspace/api/skills/' .gitignore && echo ".gitignore negation rule present"</automated>
  </verify>
  <done>
    - 3 Skill directories created under workspace/api/skills/ with SKILL.md files
    - Each SKILL.md has valid frontmatter (name, description, version)
    - Skills content matches classroom reference (MASTEST methodology)
    - .gitignore has negation rule for workspace/api/skills/
    - All Skill files tracked in git
  </done>
</task>

</tasks>

<verification>
1. Import check: `.venv/Scripts/python -c "from src.app.agents.api.tools import MASTEST_TOOLS, composite_backend, file_backend; print(f'OK: {len(MASTEST_TOOLS)} tools')"`
2. Config check: `.venv/Scripts/python -c "from src.app.core.config import settings; assert hasattr(settings, 'gitnexus_mcp_command'); print('GitNexus config OK')"`
3. Skills check: All 3 SKILL.md files exist with correct frontmatter
4. Git tracking: `git ls-files workspace/api/skills/` shows 3 files
</verification>

<success_criteria>
- MASTEST_TOOLS list contains parse_openapi_spec + check_script_syntax + compute_coverage + playwright_api_tools
- CompositeBackend configured at workspace/api/ with shell + file backends
- Levenshtein installed and importable
- GitNexus MCP settings in config.py
- 3 Skill SKILL.md files tracked in git
- All imports use `from src.app.` prefix
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-automation-agent/06-01-SUMMARY.md`
</output>
