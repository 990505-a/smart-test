---
phase: 02-testcase-agent-mvp
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/agents/testcase/agent.py
autonomous: true
requirements:
  - PARS-01
  - PARS-05
  - MIDW-01
  - MIDW-02
  - MIDW-03
  - MIDW-06
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-06
  - SKILL-07
  - EXPT-01
  - EXPT-02
  - EXPT-04

must_haves:
  truths:
    - "TestCase agent loads with SkillsMiddleware + PDFContextMiddleware in onion layers"
    - "Agent system prompt enforces 5-stage mandatory workflow with quality red-lines"
    - "SkillsMiddleware loads 5 SKILL.md files from src/app/skills/ directory"
    - "PDFContextMiddleware injects PDF content while preserving Skills system prompt"
    - "Excel export tool is registered as an agent tool"
    - "Agent is importable as CompiledStateGraph (no import errors)"
  artifacts:
    - path: "src/app/agents/testcase/agent.py"
      provides: "Fully wired TestCase agent with middleware, tools, and system prompt"
      exports: ["agent"]
      contains: "SkillsMiddleware"
  key_links:
    - from: "src/app/agents/testcase/agent.py"
      to: "src/app/middleware/pdf_context.py"
      via: "PDFContextMiddleware import and instantiation"
      pattern: "from.*middleware.*pdf_context.*import.*PDFContextMiddleware"
    - from: "src/app/agents/testcase/agent.py"
      to: "src/app/skills/"
      via: "SkillsMiddleware sources parameter"
      pattern: "sources=\\[.*skills"
    - from: "src/app/agents/testcase/agent.py"
      to: "src/app/agents/testcase/tools.py"
      via: "export_test_cases_to_excel tool registration"
      pattern: "export_test_cases_to_excel"
---

<objective>
Wire the TestCase agent with middleware chain, tools, and comprehensive system prompt.

Purpose: This is the integration plan that connects all Phase 2 subsystems (PDF middleware, Skills system, Excel export tool) into the TestCase agent. The agent becomes a fully functional test case generation engine.

Output: A working TestCase agent that loads skills, processes PDFs, enforces 5-stage workflow, and exports test cases to Excel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-testcase-agent-mvp/02-CONTEXT.md
@.planning/phases/02-testcase-agent-mvp/02-RESEARCH.md
@.planning/phases/02-testcase-agent-mvp/02-01-SUMMARY.md
@.planning/phases/02-testcase-agent-mvp/02-02-SUMMARY.md

<interfaces>
<!-- Interfaces from Plans 01 and 02 that this plan consumes. -->

From src/app/middleware/pdf_context.py (Plan 01):
```python
class PDFContextMiddleware(AgentMiddleware):
    def __init__(
        self,
        original_system_prompt: str | list | None = None,
        enable_cache: bool = True,
        max_content_length: int = 80_000,
    ): ...
```

From src/app/processors/pdf.py (Plan 01):
```python
class PDFProcessor:
    def __init__(self, enable_cache: bool = True): ...
    def extract_text(self, pdf_data: bytes, filename: str = "unknown.pdf") -> str: ...
```

From src/app/agents/testcase/tools.py (Plan 02):
```python
@tool
def export_test_cases_to_excel(
    test_cases: list[dict[str, Any]],
    output_path: str,
    sheet_name: str = "测试用例",
) -> str: ...
```

From src/app/skills/ (Plan 02):
5 directories: requirement-analysis, test-strategy, test-case-design, quality-review, output-formatter

From DeepAgents:
```python
from deepagents import create_deep_agent as create_agent
from deepagents.backends import FilesystemBackend
from deepagents.middleware import SkillsMiddleware

# SkillsMiddleware constructor:
# SkillsMiddleware(backend=FilesystemBackend, sources=["/skills/"])
# sources paths are relative to backend root_dir
```

Current agent.py structure (Phase 1 stub to be replaced):
```python
from pathlib import Path
from deepagents import create_deep_agent as create_agent
from deepagents.backends import FilesystemBackend
from langchain.chat_models import init_chat_model
from dotenv import load_dotenv

load_dotenv()
llm = init_chat_model("deepseek:deepseek-chat")
workspace_dir = Path(__file__).parent.parent.parent.parent.parent / "workspace"
file_backend = FilesystemBackend(root_dir=workspace_dir, virtual_mode=True)

agent = create_agent(
    model=llm,
    tools=[],
    backend=file_backend,
    middleware=[],
    system_prompt="你是智能测试平台的测试用例生成助手。... (stub placeholder)"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TestCase agent with middleware, tools, and system prompt</name>
  <files>src/app/agents/testcase/agent.py</files>
  <read_first>
    D:/test_agent/smart-test-platform/src/app/agents/testcase/agent.py
    D:/test_agent/2026-05-07-ai-test-agent-system/2026-05-07-ai-test-agent-system/ai-test-agent-system/src/app/agents/testcase/agent.py
    D:/test_agent/smart-test-platform/src/app/middleware/pdf_context.py
    D:/test_agent/smart-test-platform/src/app/agents/testcase/tools.py
    D:/test_agent/smart-test-platform/src/app/core/config.py
    D:/test_agent/smart-test-platform/src/app/core/llms.py
  </read_first>
  <action>
    Replace the entire content of src/app/agents/testcase/agent.py with the fully wired agent. The file must implement:

    1. **Imports:**
    ```python
    from pathlib import Path
    from deepagents import create_deep_agent as create_agent
    from deepagents.backends import FilesystemBackend
    from deepagents.middleware import SkillsMiddleware
    from langchain.chat_models import init_chat_model
    from dotenv import load_dotenv

    from app.middleware.pdf_context import PDFContextMiddleware
    from app.agents.testcase.tools import export_test_cases_to_excel

    load_dotenv()
    ```

    2. **LLM and Backend initialization:**
    ```python
    llm = init_chat_model("deepseek:deepseek-chat")
    workspace_dir = Path(__file__).parent.parent.parent.parent.parent / "workspace"
    file_backend = FilesystemBackend(root_dir=workspace_dir, virtual_mode=True)
    ```

    3. **SkillsMiddleware (D-05 outer layer, D-12/D-13):**
    Per RESEARCH Open Question 2 and Pitfall 1: Use a SEPARATE FilesystemBackend for skills, rooted at `src/app/`, with sources `["/skills/"]`.
    ```python
    # Skills are in src/app/skills/ - separate backend rooted at src/app/
    app_dir = Path(__file__).parent.parent.parent  # src/app/
    skills_backend = FilesystemBackend(root_dir=app_dir, virtual_mode=True)
    skills_middleware = SkillsMiddleware(
        backend=skills_backend,
        sources=["/skills/"]
    )
    ```

    4. **System prompt (D-01/D-03/D-14 - 5-stage mandatory workflow):**
    Adapt the SYSTEM_PROMPT from the reference code at `D:/test_agent/2026-05-07-ai-test-agent-system/.../agent.py`, but remove all RAG-related content (Phase 3 scope) and dynamic model selection (Phase 4 scope).

    The system prompt MUST include:
    - Role definition: enterprise-level senior test architect
    - Core workflow rules: strict 5-stage sequential execution (D-03)
    - Workflow table with Phase/Skill/Output/Entry condition columns
    - Red line: must not generate test cases before completing Phase 1 (requirement analysis) and Phase 2 (test strategy)
    - Skill activation rules for both single-skill and multi-skill scenarios
    - Quality red-lines (7 rules: traceability, verifiability, data completeness, atomicity, independence, security, boundary)
    - Unclear requirement handling rules
    - Output behavior norms
    - Language consistency (Chinese output for Chinese input)

    Remove from reference:
    - All RAG-related instructions ("先 RAG，后分析", rag-query Skill references)
    - dynamic_model_selection references
    - Context dataclass with enable_rag
    - test-data-generator Skill (Phase 4 scope, only 5 Skills per D-02)
    - RAGMiddleware import

    The 5-stage workflow table (per D-01/D-02):
    ```
    | Phase | Skill | Output | Entry Condition |
    |-------|-------|--------|-----------------|
    | Phase 1 | requirement-analysis | Requirement analysis report (feature matrix + risk list + case estimation) | User confirms or continues by default |
    | Phase 2 | test-strategy | Test strategy report (type selection + priority + depth allocation) | User confirms or continues by default |
    | Phase 3 | test-case-design | Per-module test cases | Per-module lightweight self-check |
    | Phase 4 | quality-review | Quality review report | Overall score >= 75, otherwise return to modify |
    | Phase 5 | output-formatter | Final deliverable (user-specified format) | - |
    ```

    5. **PDFContextMiddleware (D-05 inner layer):**
    ```python
    pdf_middleware = PDFContextMiddleware(
        original_system_prompt=SYSTEM_PROMPT,
        enable_cache=True,
    )
    ```

    6. **Agent creation (D-05 middleware order):**
    ```python
    agent = create_agent(
        model=llm,
        tools=[export_test_cases_to_excel],
        backend=file_backend,
        middleware=[
            skills_middleware,      # D-05 outer layer: loads SKILL.md
            pdf_middleware,         # D-05 inner layer: injects PDF context
        ],
        system_prompt=SYSTEM_PROMPT,
    )
    ```

    Key design points to get right:
    - SkillsMiddleware is OUTER layer (executes first in onion model)
    - PDFContextMiddleware is INNER layer (executes last, sees Skills content in system_message)
    - SYSTEM_PROMPT is passed to both agent creation AND PDFContextMiddleware constructor (for immutable fallback)
    - Tools list contains only export_test_cases_to_excel (no RAG tools, no PDF extraction tool -- PDF processing is handled by middleware, not tools)
  </action>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && .venv/Scripts/python -c "from src.app.agents.testcase.agent import agent; print(type(agent).__name__); assert 'CompiledStateGraph' in type(agent).__name__ or 'Pregel' in type(agent).__name__; print('Agent import OK')" && .venv/Scripts/python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - agent.py contains `from app.middleware.pdf_context import PDFContextMiddleware`
    - agent.py contains `from deepagents.middleware import SkillsMiddleware`
    - agent.py contains `from app.agents.testcase.tools import export_test_cases_to_excel`
    - agent.py contains `SkillsMiddleware(backend=skills_backend, sources=["/skills/"])`
    - agent.py contains `PDFContextMiddleware(original_system_prompt=SYSTEM_PROMPT`
    - agent.py contains `middleware=[skills_middleware, pdf_middleware]`
    - agent.py contains `tools=[export_test_cases_to_excel]`
    - agent.py contains the 5-stage workflow table with requirement-analysis, test-strategy, test-case-design, quality-review, output-formatter
    - agent.py does NOT contain "rag-query" or "RAGMiddleware" or "enable_rag" (those are Phase 3)
    - agent.py does NOT contain "dynamic_model" or "image_llm_model" (those are Phase 4)
    - agent imports successfully as CompiledStateGraph
    - All existing tests continue to pass
  </acceptance_criteria>
  <done>
    - TestCase agent loads with SkillsMiddleware + PDFContextMiddleware in onion layers (D-05)
    - SkillsMiddleware configured with separate backend rooted at src/app/, sources=["/skills/"]
    - PDFContextMiddleware receives SYSTEM_PROMPT as original_system_prompt for immutable pattern (D-06/MIDW-03)
    - System prompt enforces 5-stage mandatory workflow (D-01/D-03/D-14/SKILL-07)
    - Excel export tool registered as agent tool
    - Agent is importable without errors
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
```bash
cd D:/test_agent/smart-test-platform
# Verify agent imports correctly
.venv/Scripts/python -c "from src.app.agents.testcase.agent import agent; print('Agent type:', type(agent).__name__)"
# Verify all tests pass
.venv/Scripts/python -m pytest tests/ -v --tb=short
```
Agent must import successfully and all tests must pass.
</verification>

<success_criteria>
- TestCase agent loads with 2-layer onion middleware (SkillsMiddleware outer, PDFContextMiddleware inner)
- SkillsMiddleware configured to load from src/app/skills/ with 5 SKILL.md files
- PDFContextMiddleware receives SYSTEM_PROMPT for immutable system prompt pattern
- System prompt enforces 5-stage mandatory workflow with quality red-lines
- Excel export tool registered as the only agent tool
- Agent imports as CompiledStateGraph without errors
- All existing test suites pass (test_pdf_processor, test_pdf_middleware, test_skills, test_excel_export)
</success_criteria>

<output>
After completion, create `.planning/phases/02-testcase-agent-mvp/02-03-SUMMARY.md`
</output>
