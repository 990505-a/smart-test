---
phase: 10-agent-database-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/agents/testcase/tools/db_tools.py
  - src/app/agents/testcase/agent.py
  - src/app/agents/testcase/tools/__init__.py
  - src/app/skills/output-formatter/SKILL.md
  - src/app/skills/quality-review/SKILL.md
autonomous: true
requirements:
  - PLAT-14
  - PLAT-16

must_haves:
  truths:
    - "Agent automatically calls save_test_cases_batch at end of 5-stage workflow"
    - "Agent creates a default project if none exists for the workspace"
    - "Agent pauses and asks user before destructive operations (delete, overwrite, execute)"
    - "Agent returns structured save result with project_id, case count, and identifiers"
  artifacts:
    - path: "src/app/agents/testcase/tools/db_tools.py"
      provides: "ensure_project tool for auto-creating projects"
      contains: "ensure_project"
    - path: "src/app/agents/testcase/agent.py"
      provides: "Agent with DB tools registered and updated system prompt"
      contains: "save_test_cases_batch"
    - path: "src/app/skills/output-formatter/SKILL.md"
      provides: "Auto-save instructions in output stage"
      contains: "save_test_cases_batch"
    - path: "src/app/skills/quality-review/SKILL.md"
      provides: "HITL checkpoint for destructive operations"
      contains: "HITL"
  key_links:
    - from: "src/app/agents/testcase/agent.py"
      to: "src/app/agents/testcase/tools/db_tools.py"
      via: "import and tools= list registration"
      pattern: "db_tools\\.(save_test_cases_batch|ensure_project)"
    - from: "src/app/agents/testcase/agent.py"
      to: "src/app/skills/output-formatter/SKILL.md"
      via: "SkillsMiddleware loads SKILL.md content into system prompt"
      pattern: "SkillsMiddleware"
---

<objective>
Wire the TestCase Agent to auto-save generated test cases to the database and add HITL checkpoints for destructive operations.

Purpose: Per D-01, Agent auto-saves test cases at end of workflow via save_test_cases_batch. Per D-02, auto-save targets current project or creates one. Per D-03/D-04, HITL via Agent text prompts for destructive operations only. This connects the Agent's generation capability (Phases 2-6) to the database (Phase 8).

Output: Updated agent.py with DB tools, ensure_project helper, updated SKILL.md files with auto-save and HITL instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-database-integration/10-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From src/app/agents/testcase/tools/db_tools.py:
```python
@tool
async def save_test_cases_batch(
    project_id: str,
    test_cases: list[dict],
    folder_id: str | None = None,
) -> dict:
    """Returns {success, total_count, saved_ids, errors}"""

@tool
async def save_test_case_to_db(
    project_id: str,
    name: str,
    steps: list[dict],
    priority: str = "medium",
    folder_id: str | None = None,
    # ... more params
) -> dict:
    """Returns {success, test_case_id, identifier, steps_count}"""

@tool
async def list_project_test_cases(
    project_id: str,
    limit: int = 50,
) -> dict:
    """Returns {success, test_cases, count}"""
```

From src/app/db/models/project.py:
```python
DEFAULT_USER_ID = UUID("00000000-0000-0000-0000-000000000001")
# Project model has: id, identifier, name, description, created_by, created_at, updated_at
```

From src/app/agents/testcase/agent.py:
```python
agent = create_agent(
    model=llm,
    tools=[export_test_cases] + wiki_tools,
    backend=file_backend,
    middleware=[skills_middleware, dynamic_model_middleware, file_middleware],
    system_prompt=SYSTEM_PROMPT,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ensure_project tool and register DB tools in agent</name>
  <files>src/app/agents/testcase/tools/db_tools.py, src/app/agents/testcase/agent.py</files>
  <read_first>
    - src/app/agents/testcase/tools/db_tools.py
    - src/app/agents/testcase/agent.py
    - src/app/db/models/project.py
    - src/app/core/workspace.py
  </read_first>
  <action>
    **1. Add `ensure_project` tool to db_tools.py:**

    Add a new `@tool` function `ensure_project` after the existing tools:
    ```python
    @tool
    async def ensure_project(project_name: str = "AI Generated Project") -> dict:
        """Ensure a project exists for the current workspace. If no project exists, create one automatically.

        Call this before save_test_cases_batch if you don't have a project_id.

        Args:
            project_name: Name for auto-created project (default "AI Generated Project").

        Returns:
            Dict with success, project_id, identifier, project_name, and is_new (True if just created).
        """
    ```
    Implementation:
    - Use `async_session_factory()` to query `Project` table with `limit=1`
    - If a project exists, return it (success=True, is_new=False, project_id=str(project.id), project_name, identifier)
    - If no project exists, create one:
      - Use `generate_identifier_simple("PR")` for the identifier
      - Create `Project(identifier=..., name=project_name, created_by=DEFAULT_USER_ID)`
      - Return (success=True, is_new=True, project_id, project_name, identifier)
    - On error, return {success: False, error: str(e)}

    **2. Register DB tools in agent.py:**

    In `src/app/agents/testcase/agent.py`:
    - Add import: `from app.agents.testcase.tools.db_tools import save_test_cases_batch, save_test_case_to_db, list_project_test_cases, ensure_project`
    - Update the `agent = create_agent(...)` call's `tools=` parameter:
      ```python
      tools=[export_test_cases, save_test_cases_batch, save_test_case_to_db, list_project_test_cases, ensure_project] + wiki_tools,
      ```

    **3. Update system prompt SYSTEM_PROMPT in agent.py:**

    Add the following section after the "输出行为规范" section and before the closing line:

    ```
    ---

    # 自动保存规范（Phase 10）

    ## 保存时机
    在 Phase 5（output-formatter）完成输出后，**必须**自动调用以下流程保存到数据库：

    1. 先调用 `ensure_project` 获取或创建项目（如果已有 project_id 可跳过）
    2. 将所有生成的测试用例转换为 `save_test_cases_batch` 所需格式
    3. 调用 `save_test_cases_batch` 保存到数据库

    ## 保存结果格式
    保存完成后，在回复中输出以下格式的汇总信息（供前端卡片渲染）：

    [SAVE_RESULT]
    status: success
    project_id: {project_id}
    project_name: {project_name}
    case_count: {count}
    identifiers: {id1}, {id2}, ...
    [/SAVE_RESULT]

    如果保存失败：
    [SAVE_RESULT]
    status: error
    error: {error_message}
    [/SAVE_RESULT]

    ## 数据库保存格式
    将 Markdown 格式的测试用例转换为以下结构传给 `save_test_cases_batch`：
    ```json
    {
      "project_id": "从 ensure_project 获取",
      "test_cases": [
        {
          "name": "用例标题",
          "description": "用例描述",
          "preconditions": "前置条件文本",
          "priority": "medium|high|low|critical",
          "test_case_type": "functional",
          "template": "test_case",
          "steps": [
            {"action": "具体操作", "expected_result": "预期结果"},
            ...
          ]
        }
      ],
      "folder_id": "可选，不指定则保存到项目根目录"
    }
    ```

    ## Human-in-the-Loop 交互规范

    以下破坏性操作**必须**先暂停并询问用户确认：
    - 删除测试用例："即将删除 N 条测试用例，是否继续？"
    - 覆盖已有数据："该操作将覆盖已有的 N 条用例，是否继续？"
    - 执行测试脚本："即将执行测试脚本，可能影响目标系统，是否继续？"

    非破坏性操作（生成、保存、查询）**自动执行**，无需确认。
    ```

    Remove the last line `请始终以企业级测试工程师的专业标准执行每一个任务。` and keep the rest of the prompt unchanged.
  </action>
  <acceptance_criteria>
    - db_tools.py contains `async def ensure_project` with @tool decorator
    - agent.py imports db_tools functions: save_test_cases_batch, save_test_case_to_db, list_project_test_cases, ensure_project
    - agent.py tools list includes all 4 DB tools + export_test_cases + wiki_tools
    - SYSTEM_PROMPT contains "[SAVE_RESULT]" marker pattern
    - SYSTEM_PROMPT contains "ensure_project" reference
    - SYSTEM_PROMPT contains "Human-in-the-Loop" section
  </acceptance_criteria>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && python -c "from src.app.agents.testcase.tools.db_tools import ensure_project, save_test_cases_batch, save_test_case_to_db, list_project_test_cases; print('DB tools import OK')" && python -c "from src.app.agents.testcase.agent import agent; tool_names = [t.name for t in agent.tools]; assert 'save_test_cases_batch' in tool_names, f'Missing save_test_cases_batch in {tool_names}'; assert 'ensure_project' in tool_names, f'Missing ensure_project in {tool_names}'; print('Agent tools registered OK')" && grep -c "SAVE_RESULT" src/app/agents/testcase/agent.py</automated>
  </verify>
  <done>Agent has ensure_project, save_test_cases_batch, save_test_case_to_db, list_project_test_cases registered as tools. System prompt includes auto-save instructions with [SAVE_RESULT] markers and HITL rules for destructive operations.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md files with auto-save and HITL instructions</name>
  <files>src/app/skills/output-formatter/SKILL.md, src/app/skills/quality-review/SKILL.md</files>
  <read_first>
    - src/app/skills/output-formatter/SKILL.md
    - src/app/skills/quality-review/SKILL.md
  </read_first>
  <action>
    **1. Update output-formatter/SKILL.md:**

    Append the following section at the end of the file (before any trailing `---`):

    ```markdown
    ---

    ## 自动保存到数据库

    在完成所有测试用例的格式化输出后，**必须**执行以下自动保存流程：

    ### 保存步骤
    1. 调用 `ensure_project` 获取当前项目的 project_id（如已有则直接使用）
    2. 将格式化后的所有测试用例转换为 `save_test_cases_batch` 所需的 JSON 格式
    3. 调用 `save_test_cases_batch(project_id, test_cases)` 保存到数据库
    4. 在回复中输出 `[SAVE_RESULT]...[/SAVE_RESULT]` 格式的保存结果

    ### 用例格式转换规则
    将 Markdown 格式的测试用例提取为以下结构：
    - `name`: 用例标题（不含编号前缀）
    - `steps`: 每个步骤提取为 `{"action": "操作描述", "expected_result": "预期结果"}`
    - `priority`: 从 P0/P1/P2/P3 映射为 critical/high/medium/low
    - `description`: 用例描述或测试目的
    - `preconditions`: 前置条件文本

    ### 注意事项
    - 保存是非破坏性操作，直接执行无需询问用户确认
    - 如果保存失败，输出 error 格式的 SAVE_RESULT 并建议用户在管理页面手动创建
    - 每次保存后，用户可在「测试用例管理」页面查看已保存的用例
    ```

    **2. Update quality-review/SKILL.md:**

    Append the following section at the end of the file:

    ```markdown
    ---

    ## Human-in-the-Loop 检查点

    在执行质量评审时，如果发现需要执行以下操作，**必须暂停并询问用户确认**：

    | 操作类型 | 确认提示 | 说明 |
    |---------|---------|------|
    | 删除用例 | "即将删除 N 条不达标用例，是否继续？" | 删除是不可逆操作 |
    | 覆盖用例 | "该评审结果将覆盖 N 条已有用例，是否继续？" | 覆盖可能导致数据丢失 |
    | 执行测试 | "即将执行测试脚本验证质量，是否继续？" | 可能影响目标系统 |

    **自动执行的操作（无需确认）**：
    - 质量评审打分
    - 生成改进建议
    - 自动保存评审结果到数据库
    - 查询已有用例进行对比
    ```
  </action>
  <acceptance_criteria>
    - output-formatter/SKILL.md contains "save_test_cases_batch" in an auto-save section
    - output-formatter/SKILL.md contains "ensure_project" reference
    - output-formatter/SKILL.md contains "[SAVE_RESULT]" marker reference
    - quality-review/SKILL.md contains "Human-in-the-Loop" section
    - quality-review/SKILL.md contains "删除用例" and "覆盖用例" as destructive operation examples
  </acceptance_criteria>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && grep -c "save_test_cases_batch" src/app/skills/output-formatter/SKILL.md && grep -c "SAVE_RESULT" src/app/skills/output-formatter/SKILL.md && grep -c "Human-in-the-Loop" src/app/skills/quality-review/SKILL.md && grep -c "删除用例" src/app/skills/quality-review/SKILL.md</automated>
  </verify>
  <done>output-formatter SKILL.md has auto-save instructions referencing save_test_cases_batch. quality-review SKILL.md has HITL checkpoint definitions for destructive operations.</done>
</task>

</tasks>

<verification>
1. DB tools import without error: `python -c "from src.app.agents.testcase.tools.db_tools import ensure_project"`
2. Agent has all DB tools registered: `python -c "from src.app.agents.testcase.agent import agent; print([t.name for t in agent.tools])"`
3. System prompt contains SAVE_RESULT marker: `grep "SAVE_RESULT" src/app/agents/testcase/agent.py`
4. SKILL.md files updated with auto-save and HITL sections
</verification>

<success_criteria>
- Agent tools list includes: save_test_cases_batch, save_test_case_to_db, list_project_test_cases, ensure_project, export_test_cases
- System prompt instructs agent to auto-call ensure_project then save_test_cases_batch at end of workflow
- System prompt defines [SAVE_RESULT]...[/SAVE_RESULT] output format for frontend card parsing
- SKILL.md files include HITL checkpoints for destructive operations
- All imports resolve without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-database-integration/10-01-SUMMARY.md`
</output>
