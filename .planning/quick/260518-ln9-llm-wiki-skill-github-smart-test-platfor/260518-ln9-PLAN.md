---
phase: quick
plan: 260518-ln9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/skills/wiki-query/SKILL.md
autonomous: true
requirements: [SKILL-ALIGNMENT]
must_haves:
  truths:
    - "SKILL.md parameter tables match actual wiki-mcp tool schemas exactly"
    - "SKILL.md documents the wiki parameter present on all multi-wiki tools"
    - "SKILL.md documents the tags array parameter on search and list_pages"
    - "SKILL.md documents the source parameter on graph_query trace_source mode"
    - "SKILL.md documents the limit parameter on graph_query neighbors mode"
    - "graph_query uses 'page' not 'node' as parameter name"
    - "SKILL.md mentions MCP Resources (wiki://{name}/purpose etc) as available context"
  artifacts:
    - path: "src/app/skills/wiki-query/SKILL.md"
      provides: "Updated wiki-query skill aligned with actual wiki-mcp tool implementations"
      contains: "wiki:"
  key_links:
    - from: "src/app/skills/wiki-query/SKILL.md"
      to: "wiki-mcp tools (search, get_page, list_pages, list_wikis, graph_query, reload)"
      via: "parameter schemas must match actual tool inputSchema definitions"
      pattern: "wiki.*parameter"
---

<objective>
Update the wiki-query SKILL.md to accurately reflect the actual wiki-mcp tool implementations.

Purpose: The current SKILL.md has several parameter inaccuracies that could mislead the LLM agent when constructing tool calls. Specifically: missing `wiki` parameter on multi-wiki tools, wrong parameter name (`node` vs `page`) in graph_query, missing `tags` array parameter on search/list_pages, missing `source`/`limit` parameters on graph_query modes, and no mention of MCP Resources. Aligning the skill with the actual implementation ensures the agent calls tools correctly.

Output: Updated `src/app/skills/wiki-query/SKILL.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md tool parameter tables to match actual wiki-mcp schemas</name>
  <files>src/app/skills/wiki-query/SKILL.md</files>
  <action>
    Update the wiki-query SKILL.md to align all 6 tool parameter tables with the actual wiki-mcp source code at D:/llm-wiki/wiki-mcp/src/tools/. Changes required:

    **1. list_wikis** -- No changes needed (no parameters, matches actual).

    **2. search** -- Update parameter table:
    - Add `wiki` parameter: `string, optional, Wiki project name. Omit to use the default wiki.`
    - Rename `type` description to match actual: "Filter by page type: entity, concept, source, query, synthesis, comparison."
    - Replace `maxTokens` description with: "Maximum tokens in response. Default: 4000."
    - Add `tags` parameter: `string[], optional, Filter by tags. Pages must have ALL specified tags.`
    - Update description to mention graph-expanded results with `[graph]` tag prefix.

    **3. get_page** -- Update parameter table:
    - Add `wiki` parameter: `string, optional, Wiki project name. Omit to use the default (first) wiki.`
    - Update `path` description to: "Page path (from list_pages output), page title, or filename stem. Supports fuzzy matching."
    - Update `related` description to: "Include top 3 graph-correlated pages."

    **4. list_pages** -- Update parameter table:
    - Add `wiki` parameter: `string, optional, Wiki project name. Omit to use the default (first) wiki.`
    - Add `tags` parameter: `string[], optional, Filter by tags. Pages must have ALL specified tags.`
    - Remove the current `tag` (singular) parameter if present -- the actual tool uses `tags` (plural, array type).

    **5. graph_query** -- MAJOR corrections needed:
    - Add `wiki` parameter: `string, optional, Wiki project name. Omit to use the default wiki.`
    - Add `maxTokens` parameter: `number, optional, Maximum tokens in response. Default: 4000.`
    - Add `limit` parameter: `number, optional, Max results for neighbors mode. Default: 10.`
    - RENAME `node` to `page`: The actual parameter is `page` (not `node`). Description: "Page path or title for neighbors mode."
    - Add `source` parameter: `string, optional, Source filename for trace_source mode."`
    - Update the query modes table to show correct parameter requirements:
      - neighbors: requires `page` (not `node`)
      - stats: no additional params needed
      - trace_source: requires `source` (not just a generic description)

    **6. reload** -- Update:
    - Add `wiki` parameter: `string, optional, Wiki project name to reload. Omit to reload all wikis.`

    **7. Add MCP Resources section** after the 6 tools, before the workflow integration section:
    - Document that wiki-mcp exposes 4 MCP Resources via URI templates:
      - `wiki://{name}/purpose` -- Why this wiki exists and its research goals
      - `wiki://{name}/schema` -- Page type definitions and field descriptions
      - `wiki://{name}/overview` -- High-level content overview
      - `wiki://{name}/index` -- Page index with links to all wiki pages
    - Note: Resources are read via MCP resource protocol, not tool calls. They provide additional context about a wiki's purpose and structure.

    **8. Update the decision tree** at the bottom:
    - Fix `graph_query(neighbors)` line to use `page=` instead of just mentioning it
    - Add mention of tags filter availability on search and list_pages

    Keep ALL existing content that is still accurate (activation scenarios, workflow integration sections, query strategy suggestions, output format, quality standards). Only modify the tool parameter tables and add the Resources section.
  </action>
  <verify>
    <automated>cd D:/test_agent/smart-test-platform && grep -c "wiki:" src/app/skills/wiki-query/SKILL.md && grep "page.*string.*optional" src/app/skills/wiki-query/SKILL.md | head -3 && grep "tags.*string\|tags.*array" src/app/skills/wiki-query/SKILL.md | head -3 && grep "source.*string.*optional" src/app/skills/wiki-query/SKILL.md | head -2 && grep -c "MCP Resource" src/app/skills/wiki-query/SKILL.md</automated>
  </verify>
  <done>
    - All 6 tool parameter tables match the actual inputSchema in D:/llm-wiki/wiki-mcp/src/tools/*.ts
    - `wiki` parameter documented on search, get_page, list_pages, graph_query, reload
    - `tags` (array) documented on search and list_pages (not `tag` singular)
    - `page` (not `node`) used as parameter name for graph_query neighbors mode
    - `source` parameter documented for graph_query trace_source mode
    - `limit` parameter documented for graph_query neighbors mode
    - MCP Resources section added documenting wiki://{name}/purpose, schema, overview, index
    - All existing workflow integration, query strategies, and output format sections preserved
  </done>
</task>

</tasks>

<verification>
- Read the updated SKILL.md and verify every parameter table matches the corresponding source file in D:/llm-wiki/wiki-mcp/src/tools/
- Confirm no parameter names from the old version remain (e.g., `node`, `tag` singular)
- Confirm the file still has valid YAML frontmatter and renders correctly as markdown
</verification>

<success_criteria>
- SKILL.md parameter tables are an exact superset of the actual wiki-mcp tool schemas
- Agent will generate correct tool calls when following the skill instructions
- No misleading or incorrect parameter names remain
</success_criteria>

<output>
After completion, create `.planning/quick/260518-ln9-llm-wiki-skill-github-smart-test-platfor/260518-ln9-SUMMARY.md`
</output>
