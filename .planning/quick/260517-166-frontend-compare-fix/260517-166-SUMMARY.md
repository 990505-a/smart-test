---
quick_id: 260517-166
description: 全面对比课堂前端代码与我们的前端实现，找出差距并修复
date: 2026-05-16
status: completed
---

# Quick Task 260517-166: Frontend Compare & Fix

## What was done

Compared classroom frontend (2026-04-09 and 2026-05-13 versions) with our implementation and fixed 4 functional gaps:

### Task 1: Create MarkdownContent component
- Created `MarkdownContent.tsx` with:
  - `react-syntax-highlighter` lazy-loaded via IntersectionObserver (only loads when code block enters viewport)
  - `oneDark` theme for syntax-highlighted fenced code blocks
  - Dedicated `StreamingMarkdownContent` component that parses Markdown progressively during streaming (headings, lists, tables, code blocks, blockquotes, task lists)
  - Rich component overrides for links (new tab), tables (bordered), blockquotes (styled), lists (spaced), images (lazy loading)
  - `bg-muted` for inline code (was unstyled before)
- Installed `react-syntax-highlighter` + `@types/react-syntax-highlighter`

### Task 2: Update ChatMessage + ToolCallBox
- Replaced inline `ReactMarkdown` with `MarkdownContent` component in ChatMessage
- Streaming AI messages now render parsed Markdown progressively instead of raw text with blinking cursor
- Non-streaming AI messages get full syntax highlighting
- Filtered out "task" tool calls from ToolCallBox display (matching classroom pattern)
- ToolCallBox auto-expands when it has a result (equivalent to classroom's `!!uiComponent || !!actionRequest`)
- Added `border-none shadow-none outline-none` styling to ToolCallBox matching classroom
- Added `disabled:cursor-default` to button matching classroom

## Files modified
- `webui/package.json` — added react-syntax-highlighter, @types/react-syntax-highlighter
- `webui/src/app/components/MarkdownContent.tsx` — NEW: full Markdown renderer with streaming support
- `webui/src/app/components/ChatMessage.tsx` — replaced ReactMarkdown with MarkdownContent, filter task calls
- `webui/src/app/components/ToolCallBox.tsx` — auto-expand with result, classroom styling

## Deferred (requires backend support)
- SubAgentIndicator — needs sub-agent routing in backend
- GenUI/LoadExternalComponent — needs UI-based tool support
- FilesPopover / tasks sidebar — needs todos/files state from agent
