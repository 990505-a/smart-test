---
phase: quick
plan: 260517-166
type: execute
wave: 1
depends_on: []
files_modified:
  - webui/package.json
  - webui/src/app/components/MarkdownContent.tsx
  - webui/src/app/components/ChatMessage.tsx
  - webui/src/app/components/ChatInterface.tsx
  - webui/src/app/components/ToolCallBox.tsx
autonomous: true
requirements: [FRONTEND-RENDERING]
must_haves:
  truths:
    - "AI messages render with full Markdown including syntax-highlighted code blocks"
    - "Tool call boxes skip 'task' tool calls and expand by default when they have content"
    - "Streaming AI messages render Markdown progressively, not raw text"
    - "Code blocks in AI responses have syntax highlighting with oneDark theme"
  artifacts:
    - path: "webui/src/app/components/MarkdownContent.tsx"
      provides: "Dedicated Markdown renderer with syntax highlighting"
      exports: ["MarkdownContent"]
    - path: "webui/src/app/components/ChatMessage.tsx"
      provides: "Chat message rendering with MarkdownContent for AI messages"
    - path: "webui/src/app/components/ToolCallBox.tsx"
      provides: "Tool call display that skips task calls and auto-expands"
  key_links:
    - from: "webui/src/app/components/ChatMessage.tsx"
      to: "webui/src/app/components/MarkdownContent.tsx"
      via: "import MarkdownContent"
      pattern: "MarkdownContent"
---

<objective>
Upgrade frontend chat rendering to match classroom quality.

Purpose: Our ChatMessage and ToolCallBox components lag behind the classroom reference in Markdown rendering quality, code block syntax highlighting, streaming display, and tool call filtering. This plan closes those functional gaps.

Output: MarkdownContent component with syntax highlighting, updated ChatMessage/ToolCallBox with classroom-quality rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Classroom reference files (already analyzed, not re-read):
- Classroom MarkdownContent: uses react-syntax-highlighter with oneDark, custom component overrides for code/pre/table/blockquote/ul/ol/a/img
- Classroom ChatMessage: uses extractStringFromMessageContent, renders AI text via MarkdownContent, skips "task" tool calls, shows SubAgentIndicator
- Classroom ToolCallBox: auto-expands when has uiComponent or actionRequest, skips "task" calls in parent
- Our ChatMessage: inline ReactMarkdown with basic prose styling, raw text during streaming, renders "task" calls
- Our ToolCallBox: always starts collapsed, no task-call filtering
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MarkdownContent component with syntax highlighting</name>
  <files>webui/package.json, webui/src/app/components/MarkdownContent.tsx</files>
  <action>
1. Install react-syntax-highlighter dependency:
   ```
   cd webui && npm install react-syntax-highlighter @types/react-syntax-highlighter
   ```

2. Create `webui/src/app/components/MarkdownContent.tsx` following the classroom reference pattern:
   - Import ReactMarkdown, remarkGfm, Prism as SyntaxHighlighter, oneDark style
   - Build a React.memo component accepting `{ content: string; className?: string }`
   - Apply comprehensive Tailwind prose overrides matching classroom (headings mt/mb, p mb, list pl, etc.)
   - Custom `code` component: detect language-xxx className, use SyntaxHighlighter with oneDark for fenced code blocks, inline `<code>` with bg-surface for inline code
   - SyntaxHighlighter config: wrapLines=true, wrapLongLines=true, custom fontSize 0.875rem, max-width 100%
   - Custom `pre` component: wrapper div with overflow-hidden
   - Custom `a` component: target="_blank", rel="noopener noreferrer", hover underline
   - Custom `blockquote`: border-l-4, italic, reduced opacity
   - Custom `ul`/`ol`: pl-6, li mb-1, last:mb-0
   - Custom `table`: border-collapse, td/th borders, th bg, overflow-x-auto wrapper
   - Custom `img`: max-w-full, rounded-md, lazy loading

This matches the classroom MarkdownContent.tsx structure exactly but uses our import paths (@/lib/utils for cn).
  </action>
  <verify>
    <automated>cd /d/test_agent/smart-test-platform/webui && npx tsc --noEmit --pretty 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>MarkdownContent component exists with syntax highlighting. react-syntax-highlighter installed. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update ChatMessage to use MarkdownContent and fix streaming + tool call rendering</name>
  <files>webui/src/app/components/ChatMessage.tsx, webui/src/app/components/ChatInterface.tsx, webui/src/app/components/ToolCallBox.tsx</files>
  <action>
Update ChatMessage.tsx:
1. Replace the direct ReactMarkdown import with MarkdownContent import:
   ```
   import { MarkdownContent } from "@/app/components/MarkdownContent";
   ```
   Remove direct `import ReactMarkdown` and `import remarkGfm` since MarkdownContent handles it.

2. For AI messages (non-streaming), replace the `<ReactMarkdown>` block with:
   ```
   <MarkdownContent content={displayContent} />
   ```
   This replaces the entire `<div className="prose prose-sm ..."><ReactMarkdown ...>{displayContent}</ReactMarkdown></div>` block.

3. For streaming AI messages, replace the raw `<p>` with:
   ```
   <MarkdownContent content={displayContent} className="[&_p:last-child]:inline [&_p:not(:last-child)]:inline-block" />
   <span className="animate-pulse">|</span>
   ```
   This gives progressive Markdown rendering during streaming instead of raw text.

4. Skip rendering "task" tool calls in the toolCalls map (matching classroom pattern):
   In the tool calls rendering section, add filter:
   ```
   {toolCalls.filter(tc => tc.name !== "task").map((tc) => (
     <ToolCallBox key={tc.id} toolCall={tc} />
   ))}
   ```

Update ChatInterface.tsx:
5. In processedMessages useMemo, mark tool calls with name "task" status as skipped -- no changes needed here since filtering happens in ChatMessage render. BUT: add the `interrupt` status handling that classroom has. In the toolCallsWithStatus map, add interrupt detection:
   ```
   status: "pending" as const,
   ```
   Our version already has this correct -- no change needed. Verify the interrupt status is not missing.

Update ToolCallBox.tsx:
6. Change initial expanded state to auto-expand when the tool call has a result (completed status):
   ```
   const [isExpanded, setIsExpanded] = useState(() => !!toolCall.result);
   ```
   Classroom expands when `!!uiComponent || !!actionRequest` -- we do not have these, so expanding when result exists is the equivalent behavior.

7. Remove the "field-sizing-content" consideration -- our textarea does not use it, which is fine.

8. Keep the TOOL_DISPLAY map and existing structure -- it is a good addition over classroom.

9. Add shadow-none and outline-none to the outer wrapper div to match classroom:
   ```
   "w-full overflow-hidden rounded-lg border-none shadow-none outline-none transition-colors duration-200 hover:bg-accent"
   ```

10. In the button className, add `disabled:cursor-default` matching classroom, and ensure `border-none` is present.
  </action>
  <verify>
    <automated>cd /d/test_agent/smart-test-platform/webui && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>AI messages render with MarkdownContent (syntax-highlighted code blocks). Streaming shows parsed Markdown. "task" tool calls are hidden from the tool call list. ToolCallBox auto-expands when result exists. No TypeScript errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd webui && npx tsc --noEmit`
2. MarkdownContent renders code blocks with oneDark syntax highlighting
3. Streaming AI messages show parsed Markdown, not raw text
4. "task" tool calls do not appear as expandable boxes
5. ToolCallBox with result auto-expands on mount
</verification>

<success_criteria>
- react-syntax-highlighter installed and MarkdownContent component created
- AI message Markdown rendering matches classroom quality (code blocks highlighted, tables styled, links open new tab)
- Streaming mode renders Markdown progressively
- "task" tool calls hidden from UI
- ToolCallBox auto-expands when has result
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260517-166-frontend-compare-fix/260517-166-SUMMARY.md`
</output>
